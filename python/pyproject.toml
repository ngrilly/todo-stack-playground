[project]
name = "python-todo-app"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]",
    "aiosqlite",
    "htpy",
]

[dependency-groups]
dev = [
    "ruff",
    "ty",
]
