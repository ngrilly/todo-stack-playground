Go:
- Try using the @ syntax in sqlc
- SQLite seems to not support arguments in the order by?
- No go:generate comment in the sqlc and templ files?

Bun:
- Try ElysiaJS or Hono

Frontend:
- Clarify that Tailwind is part of the frontend stack? But that means we need JS in the toolchain?

Agentic coding:
- Enable sandboxing in OpenCode to restric it to the working directory?
- The UI produced by OpenCode + Opus 4.6 on first try based on my requirements was a disaster. Should I provide more guidelines? Would it work better with a client-side approach (React, Svelte, Solid, Vue, etc.)? Seems like it was because Tailwind was missing.
- Use Chrome DevTools MCP?
